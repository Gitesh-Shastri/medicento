<!--Main layout for order page -->
        <% include pharmacy_header %>
        <div class="content" style="margin: 10px;">
                            <div class="panel panel-default panel-table" style="padding: 2%">
                                    <div class="panel-heading" style="text-align: center">

                                    </div>
                                    <div class="panel-body">
                        <table id="porder" class="table table-striped table-hover table-fw-widget" style="width: 100%">
                            <thead>
                                <tr>
                                    <th>Order No</th>
                                    <th>Order Date</th>
                                    <th>Products</th>
                                    <th>Total</th>
                                    <th>Status</th>
                                    <th>Details</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% docu.forEach((orders) => { %>
                                <tr>
                                    <td><strong><%= orders._id %></strong></td>
                                    <td><strong><%= moment(orders.created_at).format('LLLL') %></strong></td>
                                    <td><% orders.order_items.forEach((order) => { %>
                                        <strong><%= order.medicento_name %> * <%= order.quantity %></strong><br/>
                                    <%}) %></td>
                                    <td><strong><%= orders.grand_total %></strong></td>
                                    <td><strong ><%= orders.status %></strong></td>
                                    <td><button class="btn btn-primary" data-target="#or">View</button></td>
                                </tr>
                                <% }) %>
                            </tbody>
                        </table>
                        </div>
                        </div>
                    </div>
        </div>
        <% include pharamacy_order_details.ejs %>
            <script src="js/jquery-3.3.1.min.js"></script>
            <script src="js/pharma.js"></script>
            <script src="js/dataTables.js"></script>
            <script ></script>
            <script>
                $(document).ready(function() {
                    $('#porder').DataTable();
                });
            </script>
        </body>
</html>

        <!--<div class="content" style="margin: 10px;">
                            <div class="panel panel-default panel-table" style="padding: 2%">
                                    <div class="panel-heading" style="text-align: center">
                                        Active Orders
                                    </div>
                                    <div class="panel-body">
                        <table id="porder" class="table table-striped table-hover table-fw-widget" style="width: 100%">
                            <thead>
                                <tr>
                                    <th>Order_code</th>
                                    <th>Purchased_on</th>
                                    <th>Products</th>
                                    <th>Amount</th>
                                    <th>Status</th>
                                    <th>Manage</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% docu.forEach((orders) => { %>
                                <tr>
                                    <td><strong><%= orders._id %></strong></td>
                                    <td><strong><%= moment(orders.created_at).format('LLLL') %></strong></td>
                                    <td><% orders.order_items.forEach((order) => { %>
                                        <strong><%= order.medicento_name %> * <%= order.quantity %></strong><br/>
                                    <%}) %></td>
                                    <td><strong><%= orders.grand_total %></strong></td>
                                    <td><strong ><%= orders.status %></strong></td>
                                    <td><button class="btn btn-primary" data-target="#or">View</button></td>
                                </tr>
                                <% }) %>
                            </tbody>
                        </table>
                        </div>
                        </div>
                    </div>
        </div>
        <% include pharamacy_order_details.ejs %>
            <script src="js/jquery-3.3.1.min.js"></script>
            <script src="js/pharma.js"></script>
            <script src="js/dataTables.js"></script>
            <script ></script>
            <script>
                $(document).ready(function() {
                    $('#porder').DataTable();
                });
            </script>
        </body>
</html>-->
